<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="AppTheme.Base">
        <!-- Customize your theme here. -->
    </style>

    <style name="AppTheme.Base" parent="Theme.AppCompat.Light">
        <!-- patent="Theme.AppCompat.Light.DarkActionBar" -->
        <item name="android:windowNoTitle">true</item>
        <item name="windowNoTitle">true</item>
        <item name="android:windowActionBar">false</item>
        <item name="windowActionBar">false</item>

        <item name="colorPrimary">@color/material_deep_teal_500</item>
        <item name="colorPrimaryDark">@color/material_teal_700</item>
        <item name="colorAccent">@color/material_accent_500</item>
        <item name="android:windowBackground">@color/bright_foreground_material_dark</item>
        <item name="android:textColorPrimary">@color/primary_text_default_material_light</item>
        <item name="android:textColorSecondary">@color/primary_text_default_material_dark</item>

        <item name="toolbarStyle">@style/MyToolbarStyle</item>
        <item name="android:actionMenuTextColor">@color/primary_text_default_material_dark</item>
        <item name="actionMenuTextColor">@color/primary_text_default_material_dark</item>
        <!-- set the background color of the overflow menu item (including hard key menu in the bottom) -->
        <!--<item name="android:itemBackground">@color/accent_material_light</item>-->

        <!-- set the text color of the overflow menu item in the top right -->
        <!--<item name="android:itemTextAppearance">@style/menuItemTextAppearance</item>-->

        <!-- can set the SearchView's suggestions text color -->
        <!--<item name="android:dropDownItemStyle">@style/myDropDownItemStyle</item>-->

        <!--<item name="android:dropDownListViewStyle">@style/dropDownListViewStyle.light</item>-->
        <!--<item name="android:listViewStyle">@style/myListViewStyle.light</item>-->
    </style>
    
    <style name="MyToolbarStyle" parent="Widget.AppCompat.Toolbar">
        <item name="titleTextAppearance">@style/MyToolbarTitleStyle</item>
    </style>
    <style name="MyToolbarTitleStyle" parent="TextAppearance.Widget.AppCompat.Toolbar.Title">
        <item name="android:textColor">@color/primary_text_default_material_dark</item>
    </style>

    <!--
    <style name="myListViewStyle.light" parent="android:style/Widget.ListView">
        <item name="android:divider">@color/divider_color</item>
        <item name="android:dividerHeight">1dp</item>
        <item name="android:listSelector">@drawable/list_selector</item>
        <item name="android:cacheColorHint">@android:color/transparent</item>
    </style>

    <style name="dropDownListViewStyle.light" parent="Widget.AppCompat.Light.ListView.DropDown">
        <item name="android:divider">@null</item>
        <item name="android:background">@android:color/white</item>
    </style>

    <style name="myDropDownItemStyle" parent="Widget.AppCompat.Light.DropDownItem.Spinner">
        <item name="android:textColor">@color/secondary_text_default_material_light</item>
    </style>

    <style name="menuItemTextAppearance">
        <item name="android:textSize">16sp</item>
        <item name="android:textColor">@color/primary_text_default_material_light</item>
    </style>
    -->

</resources>
